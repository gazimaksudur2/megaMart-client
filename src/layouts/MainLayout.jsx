import React from 'react';
import Navbar from '../shared/NavBar/Navbar';

const MainLayout = () => {
    return (
        <div>
            <Navbar/>
            This is main Layout.
        </div>
    );
};

export default MainLayout;